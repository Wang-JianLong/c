#include <stdio.h>
// 258二进制为100000010，内存地址由高位向低位覆盖，即a处留下的是00000001=1，b处留下的是00000010=2
// int占四个字节，指针指向第一个字节的地址 然后一个字节八个位最大能装255，258就会往int的第二个字节处进一然后在第一字节处留下二
int main(void)
{
		signed char a = 0,b = 0;
		int *p = (int *)&b;
		*p = 258;
		printf("a = %d, b = %d \n",a,b);
		return 0;
}
